-- Правки сценария регистрации от 06.09.2026.
--
-- Возраст теперь спрашивают категорией («25-40», «подростки (до 18 лет)»), а не числом:
-- людям так быстрее, а служителям для подбора группы точный возраст и не нужен.
-- Уже собранные числа сохраняем текстом как есть — терять их незачем.
ALTER TABLE users ALTER COLUMN age TYPE text USING age::text;

-- Новый вариант ответа: человек не ведёт группу сам, но готов пустить её к себе домой.
-- Дальше по сценарию он идёт тем же путём, что и «готов открыть», но координатору
-- важно видеть разницу: одному нужен ведущий, другому — место.
ALTER TABLE users DROP CONSTRAINT users_mdg_status_check;
ALTER TABLE users
  ADD CONSTRAINT users_mdg_status_check
  CHECK (mdg_status IN ('open', 'home', 'join', 'member', 'leader'));
