-- Мягкое удаление регистрации из дашборда — тем же приёмом, что уже есть у
-- групп/участников/заявок: запись остаётся в базе (номер регистрации,
-- история — ничего не стирается), просто прячется из списков и рассылок.
ALTER TABLE users ADD COLUMN archived_at timestamptz;
