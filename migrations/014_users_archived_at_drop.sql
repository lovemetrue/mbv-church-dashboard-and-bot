-- Удаление регистрации из дашборда сделали жёстким (DELETE, а не архивация) —
-- колонка из 013 больше не читается и не пишется нигде, поэтому убираем её,
-- а не оставляем мёртвым полем.
ALTER TABLE users DROP COLUMN archived_at;
